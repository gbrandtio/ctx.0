# Embedded template payload (generated by tool/pack_templates.dart; sources
# of truth live under ../../templates/). The sibling .pubignore overrides
# this for publishing so the payload ships in the package.
templates/
.dart_tool/
build/
pubspec.lock
!bin/
!bin/**
