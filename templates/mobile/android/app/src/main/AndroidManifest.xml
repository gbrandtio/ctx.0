<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Maps module: approximate location is enough for "nearby" queries;
         fine location improves marker accuracy. Managed by tool/scaffold
         (docs/INTEGRATIONS.md) — do not edit the ctx marker blocks. -->
    <!-- ctx:maps_google:begin -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <!-- ctx:maps_google:end -->
    <!-- Notifications module: Android 13+ runtime push permission. -->
    <!-- ctx:push_firebase:begin -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- ctx:push_firebase:end -->
    <!-- Image capture module: declare hardware requirement as false so it doesn't restrict installs. -->
    <!-- ctx:image_capture:begin -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <!-- ctx:image_capture:end -->
    <application
        android:label="app_template"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Maps module: key injected at build time via the MAPS_API_KEY
             manifest placeholder (docs/ENVIRONMENT_VARIABLES.md). -->
        <!-- ctx:maps_google:begin -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />
        <!-- ctx:maps_google:end -->
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
