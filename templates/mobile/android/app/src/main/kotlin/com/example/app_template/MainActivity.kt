package com.example.app_template

import io.flutter.embedding.android.FlutterFragmentActivity

// FlutterFragmentActivity (not FlutterActivity): the Stripe PaymentSheet
// requires a FragmentActivity host (docs/ENVIRONMENT_VARIABLES.md, payments module).
class MainActivity : FlutterFragmentActivity()
