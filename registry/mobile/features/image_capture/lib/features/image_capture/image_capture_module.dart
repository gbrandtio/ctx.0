import 'package:go_router/go_router.dart';

import '../../app/feature_module.dart';

/// Image capture module: device camera and gallery image picker integration.
/// Managed by the scaffolder (docs/INTEGRATIONS.md).
class ImageCaptureModule extends FeatureModule {
  const ImageCaptureModule();

  @override
  List<RouteBase> get routes => [];
}
